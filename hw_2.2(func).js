/* Переменная содержит в себе число. 
Написать скрипт который посчитает факториал этого числа.
Реализовать с помощью рекурсивной функциии. */
function fact(num) {
    if(num === 0 || num === 1) {
        return 1;
    } else {
        return num * fact(num - 1);
    }
}
console.log(fact(5));